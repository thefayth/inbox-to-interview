# Status

Project: Inbox To Interview

Slug: `inbox-to-interview`

Repository target: `thefayth/inbox-to-interview`

Status: public export prepared locally and GitHub repository created.

GitHub creation: repository created at `https://github.com/thefayth/inbox-to-interview`. Direct `git push` from this shell hit Windows credential prompt issues, so the public export was published through authenticated GitHub Contents API calls.

Runtime status from private project: local V2 command-center app can run with Postgres, Redis, web, worker, API, and phone proxy. Operational engine remains private.
